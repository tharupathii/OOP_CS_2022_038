package LW_02;

public class numberOfDigits {

    public void countDigit(int number) {

        String Snum = Integer.toString(number);
        System.out.printf("Number of digits :%d%n", Snum.length());
    }
}
